/* ---------------------------------------------------------------------------
   КОНФИГУРАЦИЯ АДМИН-ПАНЕЛИ
   ---------------------------------------------------------------------------
   ВАЖНО:
   1. Это статический сайт (GitHub Pages). Любые «учётные данные» в JS-файле
      доступны клиенту. Защита здесь — символическая, по сути обфускация.
      Реальная защита публикации — это право push в репозиторий.
   2. Меняйте логин и пароль перед публикацией.
   3. ADMIN_PATH задаёт «скрытый» путь к админ-панели — это просто имя файла,
      на который должна быть ссылка. Файл с панелью должен называться так же.
   --------------------------------------------------------------------------- */

window.ELOGOR_CONFIG = {
  // Имя файла админки (без слеша). Поменяйте здесь и переименуйте сам HTML-файл,
  // если хотите свой «секретный» путь. По умолчанию: bulbazavr-moldovan.html
  ADMIN_PATH: "bulbazavr-moldovan.html",

  // Учётные данные. Меняйте перед запуском в продакшн.
  ADMIN_LOGIN:    "admin",
  ADMIN_PASSWORD: "elogor-2025",

  // Время жизни сессии админа (мс). По умолчанию — 8 часов.
  SESSION_TTL_MS: 8 * 60 * 60 * 1000,

  // Имя ключа для localStorage с overlay-данными админки.
  OVERLAY_KEY:       "elogor:overlay",
  OVERLAY_TYPES_KEY: "elogor:overlayTypes",
  SESSION_KEY:       "elogor:adminSession"
};
